---
title: Integrations
description: Setup guides for connecting memories.sh to your AI coding tools.
---

memories.sh works with every major AI coding tool. There are two ways to integrate:

1. **Generated files** — Run `memories generate <target>` to create a native config file the tool reads automatically
2. **MCP server** — Connect the tool to the memories.sh MCP server for live, bidirectional access

Most tools support both methods. Use generated files for simplicity, MCP for live access, or both together.

## Quick Setup

The fastest way to set up all your tools at once:

```bash
memories init
```

This automatically detects installed tools and configures MCP for each one.

## Supported Tools

### Desktop/Terminal Tools

| Tool | Generated File | MCP Support | Auto-detected |
|------|---------------|-------------|---------------|
| [Claude Code](/docs/integrations/claude-code) | `CLAUDE.md` | Yes | Yes |
| [Cursor](/docs/integrations/cursor) | `.cursor/rules/memories.mdc` | Yes | Yes |
| [Windsurf](/docs/integrations/windsurf) | `.windsurf/rules/memories.md` | Yes | Yes |
| [VS Code / Copilot](/docs/integrations/copilot) | `.github/copilot-instructions.md` | Yes | Yes |
| [OpenCode](/docs/integrations/opencode) | `.agents/` / `.opencode/instructions.md` | Yes | No |
| [Factory (Droid)](/docs/integrations/factory) | `.agents/` / `.factory/instructions.md` | Yes | No |
| [Cline](/docs/integrations/cline) | `.clinerules/memories.md` | Yes | No |
| [Roo](/docs/integrations/roo) | `.roo/rules/memories.md` | Yes | No |
| [Gemini](/docs/integrations/gemini) | `GEMINI.md` | No | No |

### Web-Based Tools

| Tool | Integration Method |
|------|-------------------|
| [v0 by Vercel](/docs/integrations/v0) | MCP via SSE (Server-Sent Events) |

### Universal Target

The `agents` target generates the canonical `.agents/` directory:
- Amp
- Codex  
- Goose
- Kilo
- Kiro
- OpenCode

```bash
memories generate agents
```

## Manual MCP Setup

For tools that aren't auto-detected, add to the tool's MCP config:

```json
{
  "mcpServers": {
    "memories": {
      "command": "npx",
      "args": ["-y", "@memories.sh/cli", "serve"]
    }
  }
}
```

## Remote/Web Tools (SSE)

For web-based tools like v0 that can't spawn local processes, use SSE transport:

```bash
memories serve --sse --port 3030
```

Then configure the tool to connect to `http://localhost:3030/mcp`. See the [v0 integration guide](/docs/integrations/v0) for details.

## Config File Locations

| Tool | MCP Config Location |
|------|---------------------|
| Cursor | `.cursor/mcp.json` or `~/.cursor/mcp.json` |
| Claude Code | `.mcp.json` (project) or `~/.mcp.json` (global) |
| Windsurf | `.windsurf/mcp.json` |
| VS Code | `.vscode/mcp.json` |
| OpenCode | `.opencode/mcp.json` |
| Factory | `.factory/mcp.json` |
| Claude Desktop | `~/Library/Application Support/Claude/claude_desktop_config.json` |

See each tool's page for detailed setup instructions.
